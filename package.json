{
  "name": "local-forwarder",
  "version": "0.1.0",
  "description": "A local HTTP, HTTPS, and TZT forwarding service",
  "author": "Local Forwarder",
  "private": true,
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite --config vite.config.ts\" \"npm run build:electron && wait-on http://localhost:5173 && electron .\"",
    "build:electron": "tsc -p tsconfig.node.json && node scripts/flatten-electron.mjs",
    "build": "npm run build:electron && tsc -p tsconfig.json --noEmit && vite build",
    "test": "npm run build && node scripts/run-tests.mjs",
    "package:x64": "npm run build && electron-builder --mac dir --x64 && node scripts/create-dmg.mjs && npm run verify:package",
    "package:win:x64": "npm run build && electron-builder --win nsis --x64 && npm run verify:win",
    "verify:package": "node scripts/verify-dmg.mjs",
    "smoke": "node scripts/smoke-electron.mjs"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "concurrently": "latest",
    "electron": "latest",
    "electron-builder": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wait-on": "latest"
  }
}
