# 界面细节优化实现计划

> 面向 AI 代理的工作者：必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（- [ ]）语法来跟踪进度。

**目标：** 优化请求耗时与复制操作的布局、移除 JSON 当前路径复制按钮、统一本地变量/登录缓存输入区，以及将选择框箭头向内收紧，同时保持业务行为不变。

**架构：** 继续使用现有 React 页面和集中式 styles.css。耗时、配置辅助文案和选择框分别增加语义 class；不新增通用 React 组件，不改变 IPC、数据模型或保存逻辑。

**技术栈：** React、TypeScript、CSS、Node.js node:test、tsx。

---

## 工作树约束与基线

当前 worktree 已有用户未提交改动，尤其涉及 JsonPreviewPage.tsx、styles.css 和部分 renderer 测试。实现过程中必须保留这些改动：

- 不使用 git add .、git checkout --、git reset --hard 或覆盖式重写。
- 每次编辑前先查看目标文件当前内容；只追加本需求所需的最小 diff。
- 不对包含用户改动的实现文件做整文件暂存。实现完成后先用 git diff 检查，再由用户决定是否提交或合并。
- 本计划文件和规格文件可以单独提交；实现代码不自动提交，避免把用户原有 hunk 带入提交。

### 任务 0：确认基线

**文件：** 无代码修改。

- [ ] **步骤 1：检查工作树和差异完整性**

运行：

~~~
git status --short
git diff --check
~~~

预期：只看到既有用户改动和已提交的规格/计划，不出现空白错误；不暂存任何文件。

- [ ] **步骤 2：运行基线 renderer 测试**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts test/renderer/json-preview.test.ts test/renderer/chinese-ui.test.ts test/renderer/log-panel.test.ts test/renderer/string-tool.test.ts
~~~

预期：基线测试通过。若基线失败，记录失败测试并停止实现，先确认是否来自既有用户改动。

## 任务 1：请求响应耗时与复制操作分层

**文件：**
- 修改：test/renderer/request-panel.test.ts
- 修改：src/renderer/components/RequestPage.tsx
- 修改：src/renderer/styles.css

- [ ] **步骤 1：先写失败测试，锁定响应结构和单位**

在现有请求页测试中补充以下断言：

~~~
assert.match(source, /className="response-duration"/);
assert.match(source, /耗时[\s\S]*ms/);
assert.match(source, /className="response-copy-actions"/);
assert.match(source, /className="select-control request-transport-select"/);
~~~

- [ ] **步骤 2：运行测试确认红灯**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts
~~~

预期：失败原因是 response-duration、response-copy-actions 或共享选择框 class 尚不存在，而不是测试语法错误。

- [ ] **步骤 3：写最少 JSX 实现**

在 RequestPage.tsx 中将响应标题右侧改为以下结构，保留现有复制逻辑和 response.durationMs 数据来源：

~~~
<div className="response-actions">
  <span className="response-duration">耗时 {response?.durationMs === undefined ? "—" : response.durationMs} ms</span>
  <div className="response-copy-actions">
    <button type="button" className="secondary-button" disabled={response?.body === undefined} onClick={() => void copyResponse()}>复制应答数据</button>
    {copyStatus && <small role="status">{copyStatus}</small>}
  </div>
</div>
~~~

给协议 select 增加 select-control，保留 request-transport-select：

~~~
<select className="select-control request-transport-select" value={transport} onChange={handleTransportChange}>
  <option value="tzt">TZT TCP（原生协议）</option>
  <option value="http">HTTP /reqxml</option>
 </select>
~~~

- [ ] **步骤 4：写最少 CSS 实现布局**

在 styles.css 中为 .request-card-heading 增加 flex-wrap: wrap，并添加：

~~~
.response-actions { display: flex; align-items: center; justify-content: flex-end; flex-wrap: wrap; gap: 14px; }
.response-duration { color: var(--text-subtle); font-size: 0.72rem; white-space: nowrap; }
.response-copy-actions { display: flex; align-items: center; flex-wrap: wrap; gap: 8px; }
.response-copy-actions small { color: var(--text-success); font-size: 0.7rem; white-space: nowrap; }
~~~

- [ ] **步骤 5：运行测试确认绿灯**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts
~~~

预期：请求页测试全部通过，且请求发送、复制按钮和 ms 单位的原有断言仍通过。

## 任务 2：移除 JSON 当前路径复制按钮

**文件：**
- 修改：test/renderer/json-preview.test.ts
- 修改：src/renderer/components/JsonPreviewPage.tsx

- [ ] **步骤 1：先修改测试为目标行为**

在 JSON 核心控件测试中从必需标签列表移除 复制当前路径，并加入：

~~~
assert.doesNotMatch(source, /复制当前路径/);
assert.doesNotMatch(source, /copyCurrentPath/);
assert.doesNotMatch(source, /点击字段可复制路径/);
~~~

保留 navigator.clipboard.writeText 断言，因为复制格式化 JSON仍然存在。

- [ ] **步骤 2：运行测试确认红灯**

运行：

~~~
npx tsx --test test/renderer/json-preview.test.ts
~~~

预期：失败原因是当前组件仍包含按钮、回调或过时提示。

- [ ] **步骤 3：移除按钮和专用回调**

在 JsonPreviewPage.tsx 中删除 copyCurrentPath 函数，并从树/表格工具栏删除该按钮；保留搜索、全部展开和全部收起。

将底部提示改为不承诺复制路径，例如：

~~~
{activeView === "table"
  ? "点击对象或数组行可展开子字段；点击字段可查看当前节点。"
  : "点击字段可查看当前节点；长字符串默认截断，悬停可查看完整值。数组和对象会显示节点数量。"}
~~~

不要删除 selectedPath，因为树形/表格选中态和图片导出仍依赖它。

- [ ] **步骤 4：运行测试确认绿灯**

运行：

~~~
npx tsx --test test/renderer/json-preview.test.ts
~~~

预期：JSON 测试全部通过，格式化复制、展开收起、表格和图片导出断言不回归。

## 任务 3：统一本地变量与登录缓存输入区

**文件：**
- 修改：test/renderer/chinese-ui.test.ts
- 修改：src/renderer/components/ConfigPages.tsx
- 修改：src/renderer/styles.css

- [ ] **步骤 1：先写失败测试，验证说明文字顺序**

在现有配置页测试中增加顺序断言：

~~~
const localHint = localPage.indexOf('className="config-textarea-hint"');
const localInput = localPage.indexOf('className="local-values-input"');
const cacheHint = loginPage.indexOf('className="config-textarea-hint"');
const cacheInput = loginPage.indexOf('className="login-cache-input"');
assert.ok(localHint >= 0 && localHint < localInput);
assert.ok(cacheHint >= 0 && cacheHint < cacheInput);
~~~

- [ ] **步骤 2：运行测试确认红灯**

运行：

~~~
npx tsx --test test/renderer/chinese-ui.test.ts
~~~

预期：登录缓存页面找不到位于文本框前的共享辅助文案 class。

- [ ] **步骤 3：统一 JSX 顺序和 class**

本地变量页面使用：

~~~
<section className="panel config-textarea-panel">
  <p className="muted config-textarea-hint">可直接输入本地变量，按“键 = 值”逐行填写，离开输入框后自动保存。</p>
  <textarea className="config-textarea local-values-input" aria-label="本地变量输入" value={localText} onChange={(event) => setLocalText(event.target.value)} onBlur={() => void saveLocalValues()} placeholder="localKey = value\nAnotherKey = another value" rows={18} />
  {localError && <DismissibleError message={localError} onClose={() => setLocalError("")} />}
</section>
~~~

登录缓存页面使用同样顺序，把原本位于输入框后的缓存计数说明移到上方：

~~~
<section className="panel config-textarea-panel">
  <p className="muted config-textarea-hint">当前代理已缓存 {Object.keys(loginCache).length} 项，登录请求成功后会自动更新。</p>
  <textarea className="config-textarea login-cache-input" aria-label="登录缓存" value={cacheText} onChange={(event) => setCacheText(event.target.value)} onBlur={() => void saveLoginCache()} placeholder="Token = xxx\nSessionNo = 123" rows={14} />
  {cacheError && <DismissibleError message={cacheError} onClose={() => setCacheError("")} />}
</section>
~~~

保留现有 onBlur、parseLocalCacheText、onSaveSharedValues、onSaveLoginCache 和各自 rows/最小高度。

- [ ] **步骤 4：补充共享样式**

在 styles.css 中添加：

~~~
.config-textarea-panel { display: flex; flex-direction: column; }
.config-textarea-hint { margin: 0 0 12px; line-height: 1.55; }
.config-textarea { display: block; }
~~~

- [ ] **步骤 5：运行测试确认绿灯**

运行：

~~~
npx tsx --test test/renderer/chinese-ui.test.ts
~~~

预期：本地变量、登录缓存、长值换行和页面标题相关测试全部通过。

## 任务 4：统一选择框箭头位置

**文件：**
- 修改：test/renderer/request-panel.test.ts
- 修改：test/renderer/string-tool.test.ts
- 修改：test/renderer/log-panel.test.ts
- 修改：src/renderer/components/RequestPage.tsx
- 修改：src/renderer/components/StringToolPage.tsx
- 修改：src/renderer/components/LogPanel.tsx
- 修改：src/renderer/styles.css

- [ ] **步骤 1：先写失败测试，覆盖三个选择框场景**

在三个 renderer 测试中分别增加：

~~~
assert.match(source, /select-control/);
~~~

请求页还验证 request-transport-select 与 select-control 同时存在；日志页验证两个筛选 select 都包含 select-control；字符串工具验证处理操作 select 包含 select-control。在请求页样式断言中增加：

~~~
assert.match(styleSource, /\.select-control[^\{]*\{[^}]*appearance:\s*none/);
assert.match(styleSource, /background-position:[^;]*right\s+9px/);
~~~

- [ ] **步骤 2：运行测试确认红灯**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts test/renderer/string-tool.test.ts test/renderer/log-panel.test.ts
~~~

预期：失败原因是选择框还没有共享 class 和内缩箭头样式。

- [ ] **步骤 3：给现有选择框加共享 class**

保持每个选择框的 value、onChange 和 option 不变，只增加 class：

~~~
<select className="select-control request-transport-select" value={transport} onChange={handleTransportChange}>
<select className="select-control" value={draft.operation} onChange={handleOperationChange}>
~~~

请求页使用第一种；字符串工具和日志页使用第二种。日志页的日志类型与日志级别两个选择框都必须增加 class。

- [ ] **步骤 4：用主题文字色绘制内缩箭头**

将原本分散在 .filters select、.request-transport-select 和 .string-tool-controls select 中的选择框基础边框/背景规则收敛到 .select-control，添加：

~~~
.select-control {
  appearance: none;
  border: 1px solid var(--border-input);
  border-radius: 8px;
  padding: 9px 34px 9px 10px;
  color: var(--text-primary);
  background-color: var(--input-background);
  background-image: linear-gradient(45deg, transparent 50%, currentColor 50%), linear-gradient(135deg, currentColor 50%, transparent 50%);
  background-position: right 14px center, right 9px center;
  background-repeat: no-repeat;
  background-size: 5px 5px, 5px 5px;
  outline: none;
}
.select-control:focus { border-color: var(--border-selected); }
~~~

保留输入框的既有规则，不让选择框失去 color-scheme、键盘 Tab 或浏览器选项弹层行为。

- [ ] **步骤 5：运行测试确认绿灯**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts test/renderer/string-tool.test.ts test/renderer/log-panel.test.ts
~~~

预期：三个页面的结构和样式断言全部通过。

## 任务 5：整体验证与交接

**文件：** 无额外代码修改。

- [ ] **步骤 1：运行本次相关测试集合**

运行：

~~~
npx tsx --test test/renderer/request-panel.test.ts test/renderer/json-preview.test.ts test/renderer/chinese-ui.test.ts test/renderer/log-panel.test.ts test/renderer/string-tool.test.ts
~~~

预期：相关测试 0 失败。

- [ ] **步骤 2：运行完整构建与测试**

运行：

~~~
npm test
~~~

预期：TypeScript 构建、Vite 构建和完整测试套件成功；允许既有 Windows-only 测试在 macOS 上跳过。

- [ ] **步骤 3：检查差异与未提交文件**

运行：

~~~
git diff --check
git diff -- src/renderer/components/RequestPage.tsx src/renderer/components/JsonPreviewPage.tsx src/renderer/components/ConfigPages.tsx src/renderer/components/StringToolPage.tsx src/renderer/components/LogPanel.tsx src/renderer/styles.css test/renderer/request-panel.test.ts test/renderer/json-preview.test.ts test/renderer/chinese-ui.test.ts test/renderer/log-panel.test.ts test/renderer/string-tool.test.ts
git status --short
~~~

预期：差异只包含本需求和用户原有改动，未出现删除用户文件、整文件重写或空白错误；向用户报告实现文件仍未提交，并列出验证结果。
